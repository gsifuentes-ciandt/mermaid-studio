# =============================================================================
# BACKEND VARIABLES (server/proxy.js and netlify/functions/)
# NO VITE_ prefix - server-side only, never exposed to browser
# =============================================================================

FLOW_CLIENT_ID=your-client-id-here
FLOW_CLIENT_SECRET=your-client-secret-here
FLOW_APP_TO_ACCESS=llm-api
FLOW_TENANT=lithiadw
FLOW_AGENT=mermaid-studio

# =============================================================================
# FRONTEND VARIABLES (React app - exposed to browser)
# MUST have VITE_ prefix to be accessible in frontend code
# =============================================================================

# AI Provider Configuration
VITE_AI_PROVIDER=flow
VITE_AI_FALLBACK_PROVIDER=openai

# AI Model Settings
VITE_AI_MODEL=gpt-4.1
VITE_AI_TEMPERATURE=0.7
VITE_AI_MAX_TOKENS=4000
VITE_AI_TIMEOUT=30000

# OpenAI API Key (fallback provider)
VITE_OPENAI_API_KEY=your-openai-api-key-here

# Optional: Override proxy URL (defaults to localhost:3001 in dev, /api in prod)
# VITE_FLOW_PROXY_URL=http://localhost:3001/api/chat/completions
